This folder contains images used in this projects. While running the project either all the images should be kept in the same folder as the code
or the path of the images should be changed in the code (HTML files).
